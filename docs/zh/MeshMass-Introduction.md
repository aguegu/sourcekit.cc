# MeshMass 可编程遥控系统

**官方在线平台：**
- **全球访问**：[meshmass.com](https://meshmass.com)
- **中国大陆访问**：[meshmass.y77.cc](https://meshmass.y77.cc)

## 概述

MeshMass 是一系列经济实惠的可编程遥控模块，专为遥控爱好者、STEM教育和3D打印社区设计。该系统包括：

- **TX6A4D 发射器**：带双摇杆、旋转旋钮和按钮的可编程控制器
- **RX4M4S 接收器**：4通道直流电机 + 4通道舵机接收器，内置电机驱动

它们共同构成一个完整的无线控制系统，填补了复杂的开源硬件解决方案和昂贵的专有遥控系统之间的空白。

## 设计理念

### 为什么选择基于代码的编程？

传统的遥控解决方案存在两个主要缺陷：

**A. 复杂的开源硬件方案**
- 需要深厚的电子知识进行组装和调试
- 涉及多个组件（Arduino、RF模块、电机驱动器、电源调节）
- 需要复杂的软件工具链（Arduino IDE、平台配置）
- 初学者和爱好者的学习曲线陡峭

**B. 昂贵的“可定制”遥控器**
- 依赖复杂的菜单系统，用户体验差
- 仅限于预定义的混控选项
- 价格昂贵（$200-500+ 提供类似功能）
- 无法实现真正自定义的行为

**MeshMass：两全其美**
- **简单**：使用简洁的C代码，专注于输入/输出映射的API
- **强大**：无需硬件复杂性即可获得遥控控制的编程灵活性
- **经济**：以爱好者的价格获得专业级控制
- **易用**：基于Web的编辑器，无需复杂工具链安装
- **示例驱动**：提供全面的工作代码示例和教程库，涵盖常见用例

## 竞争优势

### 空间效率与集成
- **单PCB解决方案**：40mm × 60mm RX4M4S 替代接收器 + 4个电调 + 凌乱的布线
- **传统方法**：多块板子堆叠，布线复杂，难以安装在紧凑的3D打印空间内
- **优势**：干净集成到设计模型中，无需重新设计车身以适应组件

### 系统总成本优势
- **MeshMass TX6A4D + RX4M4S**：比单独的Flysky发射器更便宜
- **传统方案**：Flysky发射器（$30+）+ 接收器（$30）+ 4个电调（$40+）+ 布线 = $100+
- **优势**：以爱好者的价格点获得专业级控制

### 编程易用性
- **基于文本的C语言**：揭示底层数学（例如 `150 + getChannel(4) * 2 / 5`）
- **教育价值**：用户看到公式控制物理运动
- **对比可视化编程**：图形化积木掩盖数学，简单逻辑占用更多屏幕空间
- **框架编程方法**：底层代码（RTOS、RF、驱动程序）预构建；用户只需编写映射逻辑

### 可重用性与灵活性
- **轻松重新烧录**：模块可以在不同打印项目间移动
- **传统遥控器**：需要复杂的菜单导航或物理重新布线
- **固件变体**：RX4M4S（4电机 + 4舵机）、RX4M3S1N（4电机 + 3舵机 + Neopixel）、RX4M1S1N1A（4电机 + 1舵机 + Neopixel + 音频）

### 易于视觉调试
- **无需执行器的固件验证**：验证代码而无需连接示波器或逻辑分析仪（这些设备可能功能强大但危险）
- **发射器侧状态显示**：电池电压和信号强度显示在发射器OLED上（用户握在手中）
- **接收器侧输出调试**：通道值、电机输出和舵机输出显示在接收器OLED上（用于映射验证）
- **使用模式对齐的设计**：发射器在手用于系统状态监控，接收器远程用于输出行为验证
- **即时反馈**：学生实时看到代码更改如何影响物理输出

## 系统架构

### 关注点分离
MeshMass系统将发射器和接收器的职责分离：

- **TX6A4D（发射器）**：将物理输入（摇杆、旋钮、按钮）映射到16个无线通道
- **RX4M4S（接收器）**：将接收到的通道映射到物理输出（电机、舵机）

这种抽象让用户能够专注于特定构建中最重要的部分，而无需担心RF协议细节。

### 基于通道的通信
- **16个无线通道**：有符号字节（-128至127）在设备之间传输
- **灵活路由**：任何输入都可以通过简单代码映射到任何输出
- **实时性能**：50Hz更新速率匹配舵机刷新，最低延迟无线通信

### 典型信号流
```
TX6A4D 摇杆 → getStick() → setChannel() → 无线传输
                                                              ↓
RX4M4S 接收 → getChannel() → setMotor()/setServo() → 电机/舵机
```

所有映射均可通过 MeshMass 在线平台 [meshmass.com](https://meshmass.com)（全球访问）或 [meshmass.y77.cc](https://meshmass.y77.cc)（中国大陆访问）配置。

## 框架编程方法

### 预构建基础
- **底层代码已编写**：RF通信、时序、硬件驱动程序
- **内置安全功能**：连接丢失处理、安全值、硬件保护
- **实时操作系统**：管理多个任务，无需用户干预

### 专注于应用逻辑
用户只需编写对其特定车辆重要的代码：
- 发射器上的输入到通道映射
- 接收器上的通道到输出映射
- 复杂行为的混合函数
- 无需理解无线协议或PWM时序细节

### 开发工作流程
**传统开源硬件方法：**
1. 研究组件（2-4小时）
2. 订购零件，等待发货（3-7天）
3. 组装硬件，调试连接（4-8小时）
4. 编写固件，调试RF通信（6-12小时）
5. 测试和优化（2-4小时）
**总计：15-35小时，耗时3-7天**

**MeshMass方法：**
1. 插入模块和USB烧录器（2分钟）
2. 选择预构建示例或在Web编辑器中修改代码（5-15分钟）
3. 烧录和测试（1分钟）
4. 按需优化代码（5-15分钟）
**总计：10-30分钟**

*meshmass.com上提供全面的示例和教程，大多数用户可以在几分钟内找到适合其特定用例的工作代码。来自3D打印社区的初学者通常可以找到与其车辆设计匹配的即用模板。*

## 教育价值

### 数学变得生动
- **变量与函数**：学生看到 `getStick(0)` 如何读取物理摇杆位置
- **坐标系**：X/Y轴直接映射到摇杆移动（-127到127范围）
- **代数运算**：使用 `(getStick(1) + getStick(2)) / 2` 实现坦克转向混控
- **条件逻辑**：使用 `if (getButton(0)) { setChannel(0, 0); }` 实现安全功能
- **缩放与变换**：应用 `getStick(0) / 2` 降低灵敏度

### 即时物理反馈
- 代码更改 → 即时车辆响应
- 通过观察车辆移动进行调试
- **视觉调试**：在OLED显示屏上实时观察输入和通道值更新
- 通过因果关系学习编程
- 代码与物理运动之间没有抽象层

### 渐进式学习路径
1. **从预烧录套件开始**：套件设计师提供即用固件
2. **修改现有示例**：调整参数和混合行为
3. **创建自定义映射**：为独特车辆设计编写原创代码
4. **高级编程**：实现复杂逻辑和状态机

## 目标受众

### 对于遥控爱好者
- 创建昂贵遥控器无法匹配的专业级混控
- 为独特车辆实现自定义行为（坦克转向、挖掘机控制）
- 无需菜单导航 - 只需编写您需要的逻辑

### 对于STEM教育
- 通过即时物理反馈教授真正的编程概念
- 学生在几分钟内看到他们的代码控制真实硬件
- 完美平衡：对初学者足够简单，对项目足够强大

### 对于3D打印社区
- 为独特的打印创作定制控制
- 无需电子专业知识 - 专注于您的设计
- 以经济实惠的方式让自定义车辆焕发生机

## 生态系统策略

### 套件设计师支持
- 设计师如CDHCHAOREN为特定模型预烧录固件
- 社区集成：设计师在meshmass.com上获得专栏，链接到他们的商店、Makerworld页面、视频频道
- 商业模式：与3D打印社区创作者共同成长

### 未来扩展
1. **额外的固件变体**用于专业应用
2. **扩展的配件生态系统**（音频模块、LED效果）
3. **教育合作伙伴关系**与STEM项目合作
4. **与3D建模软件集成**实现自动固件生成
5. **高级混合库**用于复杂车辆行为

## 配对系统

MeshMass使用安全的一对一配对系统在发射器和接收器模块之间建立独占无线连接。配对系统确保可靠通信并防止附近其他设备的干扰。

### EEPROM存储

配对信息存储在 **EEPROM**（电可擦可编程只读存储器）中，这是一种非易失性存储器，即使断电也能保留数据。一旦发射器和接收器配对，它们的连接信息会在电源循环和重启后保持。这意味着您无需每次使用时重新配对设备——当两个设备通电时，已建立的连接会自动恢复。

### 一对一独占配对

MeshMass配对是**双向且独占的**：
- 当发射器与新接收器配对时，它将失去对之前配对的任何接收器的访问权限
- 当接收器与新发射器配对时，其之前的发射器将无法再控制它

这种一对一关系确保每个发射器一次仅控制一个接收器，每个接收器仅响应一个发射器。如果您需要同时控制多个接收器，则需要多个发射器模块。

### 配对流程

要将MeshMass发射器与接收器配对：

1. **在第一个设备上进入配对模式**：
   - 按下并按住**配对按钮**超过**5秒钟**
   - OLED屏幕将指示配对模式（通常显示“PAIRING”或类似信息）

2. **在第二个设备上进入配对模式**：
   - 当第一个设备处于配对模式时，按下并按住第二个设备上的**配对按钮**
   - 该设备也将进入配对模式并有相应的屏幕指示

3. **自动身份交换**：
   - 当两个设备都处于配对模式且在无线范围内时，它们会自动交换身份信息
   - 此过程通常在几秒钟内完成

4. **返回正常操作**：
   - 两个设备自动退出配对模式并返回正常操作
   - OLED显示屏恢复显示输入值和通道数据
   - 设备现已配对并准备使用

**取消配对**：如果设备处于配对模式，再次按下配对按钮将终止配对过程。由于没有新的配对信息被存储，先前的配对记录保持完整，当设备返回正常模式时将继续使用。

*有关设备特定的配对按钮位置和详细信息，请参阅各个产品页面。配对按钮在PCB上标记为“PAIR”，由固件控制——不能重新编程用于其他功能。*

## 入门指南

1. **选择您的模块**：[TX6A4D 发射器](/zh/MeshMass-TX6A4D) 和 [RX4M4S 接收器](/zh/MeshMass-RX4M4S)
2. **访问 MeshMass 在线平台**：访问基于Web的编程平台 [meshmass.com](https://meshmass.com)（全球访问）或 [meshmass.y77.cc](https://meshmass.y77.cc)（中国大陆访问）
3. **获取 USB 烧录器**：编程所需（单独出售）
4. **探索示例**：从常见车辆类型的预构建代码开始
5. **加入社区**：[论坛讨论](https://github.com/aguegu/sourcekit.cc/discussions) 和 [展示案例](https://space.bilibili.com/24674093/lists/6826437)

---

*MeshMass 通过可访问的编程，将专业遥控控制带入爱好者、教育者和创客的触手可及范围。*